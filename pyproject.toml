[project]
name = "truly-rally-grit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.6,<3.10"
dependencies = [
    "interpret>=0.6.2",
    "pandas>=0.25.3",
    "tubular>=0.3.0",
]

[dependency-groups]
dev = [
    "ipykernel>=5.5.6",
]
